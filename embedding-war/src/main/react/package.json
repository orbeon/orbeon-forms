{
  "name": "react-orbeon-demo",
  "version": "0.1.0",
  "dependencies": {
    "@orbeon/react": "1.0.8",
    "esbuild": "^0.20.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.2",
    "@types/react-dom": "^17.0.11",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "build": "mkdir -p dist && esbuild src/index.tsx --bundle --outfile=dist/main.js --loader:.js=jsx --format=iife"
  }
}